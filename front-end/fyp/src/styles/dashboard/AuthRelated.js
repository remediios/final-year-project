import { Alert } from "@mui/material";
import styled from "styled-components";

export const AuthVerifyAlert = styled(Alert)`
  width: 40%;
  margin: auto;
  margin-top: 1rem;
  justify-content: center;
`;
